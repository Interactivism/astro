---
/**
 * Row of 2–4 <Stat> components.
 * Renders a top border, then a horizontal grid of stats.
 */
---

<div class="not-prose my-10 py-10 border-t border-b border-subtle grid grid-cols-2 lg:grid-cols-4 gap-8">
  <slot />
</div>
